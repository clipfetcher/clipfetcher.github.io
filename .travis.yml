language: node_js
node_js:
  - "node"

cache: npm

install: npm install
script: 
  - npm run build
  - echo 'clipfetcher.com' > CNAME

deploy:
  target_branch: master
  provider: pages
  cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  on:
    branch: dev